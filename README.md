
  # Design 24h Job Finder UI

  This is a code bundle for Design 24h Job Finder UI. The original project is available at https://www.figma.com/design/IE2xtV5PJp7S2Vo9bx8jGV/Design-24h-Job-Finder-UI.

  ## Running the code

  Run `npm i` to install the dependencies.

  Run `npm run dev` to start the development server.
  